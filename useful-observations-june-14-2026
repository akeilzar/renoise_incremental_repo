also ...

instead of pitch if smashing two LR XY wav together remove manually one sample at a time
or if drawing waveform u may zoom and manually place what macros and scripts cannot figure out today for u


doubling the sample rate of a single cycle waveform may give some more of the precision u are after

also super saw process for example works really well with resampling instead for less common textures for interesting lofi hifi phasing

